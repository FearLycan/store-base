==============================================================================
 store-base — harmonogram zadań (cron)
==============================================================================

JAK TO DZIAŁA
-------------
Synchronizacja jest kolejkowa i dzieli się na dwie role:

  • PRODUCENCI  (product/refresh-prices, review/sync)
      — tylko WSTAWIAJĄ joby do kolejki (tabela sync_job). Same nie wołają
        AliExpress. Każdy sam sprawdza, co jest "przeterminowane" wg progów
        z config/params.php, więc można je odpalać częściej niż próg — nic
        się nie zdubluje.

  • WORKER  (sync/process)
      — WYKONUJE joby z kolejki: discovery sklepu, import detali przez
        Dropshipping API (galeria HD / warianty / atrybuty), recenzje, ceny.
        To on faktycznie odpytuje AliExpress. Musi chodzić często.

Token Dropshipping API odświeża się sam (klient robi refresh ~10 min przed
wygaśnięciem przy każdym imporcie). Dopóki worker chodzi, nic nie trzeba
robić ręcznie z tokenem.

Progi cadence (config/params.php):
  sync.discoveryIntervalHours    = 6    # jak często sklep jest "due" na discovery
  sync.priceRefreshIntervalDays  = 1    # po ilu dniach cena jest "stale"
  sync.reviewRefreshIntervalDays = 3    # po ilu dniach recenzje są "stale"
  sync.timeBudgetSeconds         = 3300 # jak długo JEDEN sync/process drenuje kolejkę (potem exit)
  sync.maxAttempts               = 5    # po ilu próbach job idzie w "failed"

PRZEPUSTOWOŚĆ (jak worker teraz drenuje):
  • sync/process bez limitu leci w PĘTLI aż kolejka pusta albo minie timeBudgetSeconds
    (~55 min). Mutex (GET_LOCK) trzyma jednego workera — nakładające się crony po prostu
    wychodzą, więc jeden run może obejmować wiele ticków crona. Przepustowość zależy od
    realnej pracy, nie od granulacji crona.
  • Sleep rate-limitu (2–4 s) TYLKO po jobach bijących w afiliacyjne API (product_detail,
    price_refresh). title_rewrite (LLM) i product_reviews (mtop) idą bez sleepu.
  • Kolejność claimNext: priorytet typu (title_rewrite -> price_refresh -> product_reviews
    -> product_detail -> store_discovery), potem id. Publikacja draftów nie jest już głodzona
    przez backlog importu.
  • Publikacja tytułu (draft -> active) dzieje się INLINE w jobie product_detail — nowy
    produkt idzie live w jednym jobie. Osobny job title_rewrite pozostaje tylko dla obsługi
    już zakolejkowanych z czasów sprzed tej zmiany.


------------------------------------------------------------------------------
 WORKFLOW: kuracja sklepu (co zrobić na produkcji, od zera)
------------------------------------------------------------------------------
Model: sam wybierasz zaufanego sprzedawcę i jego produkty ("wiem co promuję").
Wklejasz linki raz, a crony utrzymują je w nieskończoność (ceny, recenzje,
detale) — bez cookie, bez ręcznej obsługi.

  WAŻNE o discovery: oficjalne API AliExpress NIE potrafi wylistować produktów
  danego sprzedawcy (brak filtra po sellerze — sprawdzone empirycznie). Auto-
  wypełnienie CAŁEGO sklepu wymagałoby scrapera listingu (świeże cookie x5sec,
  kruche) — porzucone. Kuracja opiera się na ręcznym wklejaniu w adminie.

KROK 0 — konfiguracja (jednorazowo, config/params-local.php):
  - aliexpress.appKey / appSecret / trackingId   (Affiliate API — core, ceny, linki afiliacyjne)
  - aliexpress.dropshipping.appKey / appSecret   (DS API — galeria HD / warianty / atrybuty)
  Autoryzuj DS API RAZ: admin -> Settings -> Dropshipping API -> Connect
  (AE wymaga https w redirect_uri -> użyj tunelu, patrz sekcja na dole). Token
  potem odświeża się sam, dopóki worker chodzi.

KROK 1 — dodaj sklep(y) do promowania:
  admin -> Stores -> Add,  albo:
    php yii store/add "https://pl.aliexpress.com/store/1102516804" "Nazwa sklepu"
  Ustawia external_store_id = numer z /store/... — to jednocześnie affiliate
  shop_id, po którym weryfikujemy, że produkt naprawdę należy do sprzedawcy.

KROK 2 — wklej produkty sprzedawcy:
  admin -> Products -> Import -> wybierz sklep, wklej URL-e lub numeryczne ID
  (po jednym w linii, można hurtem). Kolejkuje PRODUCT_DETAIL oznaczone jako
  "trusted" — ręczny wybór przechodzi bez weryfikacji sprzedawcy (guard off).

KROK 3 — przepuść kolejkę (albo po prostu poczekaj na crona sync/process):
    php yii sync/process 100      # limit N = przetwórz najwyżej N jobów i wyjdź
    php yii sync/process          # bez limitu = drenuj aż pusto / do timeBudgetSeconds
  Produkt wchodzi jako draft i JESZCZE w tym samym jobie product_detail dostaje
  zhumanizowany tytuł i publikację (draft -> active). Od tego momentu nic nie klikasz.

KROK 4 — utrzymanie dzieje się samo (crony niżej): refresh-prices (dziennie),
  review/sync (co 2 dni), sitemap (dziennie).

NARZĘDZIA / DOBRE PRAKTYKI:
  - Audyt obcych sprzedawców (gdyby coś wpadło ze scrapera / przed guardem):
      php yii product/verify-sellers            # dry-run, wszystkie sklepy
      php yii product/verify-sellers 2          # dry-run, sklep #2
      php yii product/verify-sellers 2 1        # USUŃ obcych ze sklepu #2
    Argumenty POZYCYJNE: <storeId> <fix> <limit>  (--flagi tu nie działają).
  - Guard shop_id: automatyczne discovery odrzuca obcych sprzedawców ("Choice"
    cross-sell); ręczne wklejenie jest zaufane i przechodzi wszystko.
  - Rate limit Affiliate: "ApiCallLimit ... ban 1s" przy seriach; przedłuża się,
    gdy wołasz w trakcie bana. sync/process usypia 2-4 s tylko po jobach afiliacyjnych;
    verify-sellers ma własny backoff, ale przy dużym katalogu odpalaj cierpliwie.

------------------------------------------------------------------------------
 CRONTAB (serwer produkcyjny, Linux)
------------------------------------------------------------------------------
# Podmień /var/www/store-base na realną ścieżkę projektu.
# Cron używa strefy czasowej serwera — godziny poniżej to czas serwera.
# Logi: KAŻDA komenda ma własny plik w runtime/logs/<komenda>.log — redirect niżej
# łapie stdout/stderr (wynik + ew. fatal), a komponent `log` Yii (config/console.php)
# routuje warningi/errory tej komendy do TEGO SAMEGO pliku. Upewnij się, że katalog
# runtime/logs/ istnieje i jest zapisywalny.

# --- WORKER: drenuje kolejkę co 10 min (robi realne wywołania AE) ---------
*/10 * * * *   cd /var/www/store-base && php yii sync/process >> runtime/logs/sync.log 2>&1

# --- PRODUCENCI (tylko kolejkują joby; worker wyżej je wykonuje) ----------

# Ceny: kolejkuje odświeżenie ceny/dostępności dla produktów starszych niż 1 dzień.
# Raz na dobę (03:15).
15 3 * * *     cd /var/www/store-base && php yii product/refresh-prices >> runtime/logs/product.log 2>&1

# Recenzje: kolejkuje re-sync recenzji dla produktów starszych niż 3 dni.
# Raz na dobę (03:30) — sam ogranicza się progiem, więc codzienne odpalenie
# realnie dotknie tylko tych faktycznie przeterminowanych.
30 3 * * *     cd /var/www/store-base && php yii review/sync >> runtime/logs/review.log 2>&1

# --- UTRZYMANIE -----------------------------------------------------------

# Token Dropshipping API: proaktywne odświeżenie co 6h, niezależnie od importów.
# Klient odświeża access_token sam przy imporcie, ale to działa tylko gdy worker
# faktycznie coś importuje — przy dłuższym przestoju refresh_token mógłby wygasnąć
# i wymusić ręczny OAuth. Ten wpis trzyma łańcuch żywy nawet na bezczynnym sklepie.
0 */6 * * *    cd /var/www/store-base && php yii ds/refresh-token >> runtime/logs/ds.log 2>&1

# Sitemap: regeneruje gzipowaną mapę strony. Raz na dobę (04:00), po syncach.
0 4 * * *      cd /var/www/store-base && php yii sitemap/generate >> runtime/logs/sitemap.log 2>&1


------------------------------------------------------------------------------
 LOKALNIE (Docker na Windows)
------------------------------------------------------------------------------
Na Windows nie ma crona — użyj Harmonogramu zadań (Task Scheduler) i wołaj
te same komendy przez kontener, np.:

  docker exec store-base php yii sync/process
  docker exec store-base php yii product/refresh-prices
  docker exec store-base php yii review/sync
  docker exec store-base php yii ds/refresh-token
  docker exec store-base php yii sitemap/generate

Do szybkiego testu możesz po prostu odpalać `sync/process` ręcznie w pętli.

Uwaga wydajnościowa: `sync/process` bez limitu drenuje kolejkę w pętli aż do pustej
(albo do sync.timeBudgetSeconds), a sleep rate-limitu obowiązuje tylko po jobach
afiliacyjnych — więc po dużym imporcie kolejka schodzi ciągle, a nie porcjami 20/tick.
Żeby ograniczyć jeden przebieg, podaj limit ręcznie:

  docker exec store-base php yii sync/process 100


==============================================================================
 KOMENDY RĘCZNE / JEDNORAZOWE (NIE w cronie)
==============================================================================

# Dodaj nowy sklep źródłowy:
docker exec store-base php yii store/add "https://pl.aliexpress.com/store/1102516804" "Nazwa sklepu"

# Reimport detali WSZYSTKICH istniejących produktów przez DS API
# (np. po zmianach w mapowaniu — discovery NIE rusza istniejących).
# Potem przepuść kolejkę workerem.
docker exec store-base php yii product/reimport
docker exec store-base php yii product/reimport --storeId=2 --limit=50   # zawężone
docker exec store-base php yii sync/process 100

# Uzupełnij slug-i dla starych wierszy bez slug (jednorazowo):
docker exec store-base php yii catalog/backfill-slugs

# Utwórz konto admina:
docker exec store-base php yii user/create <username> <email>

# Ponowna autoryzacja Dropshipping API (gdyby refresh_token wygasł / przepadł):
#   1. Odpal tunel https i ustaw aliexpress.dropshipping.callbackUrl na jego adres:
#        cloudflared tunnel --url http://localhost:8000
#   2. Wejdź na admina PRZEZ adres tunelu → Settings → Dropshipping API → Connect.
#   (AE wymaga https w redirect_uri, dlatego tunel; token potem jest trwały.)
