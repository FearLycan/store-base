<?php

// Copy this file to db-local.php and adjust to your local environment.
// Values here are merged on top of config/db.php and are NOT committed to the repo.
// Defaults below match the bundled `db` service in docker-compose.yml.
return [
    'dsn' => 'mysql:host=db;dbname=yii2basic',
    'username' => 'yii2',
    'password' => 'yii2',
];
